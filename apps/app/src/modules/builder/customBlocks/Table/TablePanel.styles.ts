import styled from '@emotion/styled';
import { Typography } from '@arco-design/web-react';
import { CheckboxField } from 'easy-email-extensions';

export const TableSizeLabel = styled(Typography)`
  margin-bottom: 5px;
`;

export const BorderCheckboxSection = styled(CheckboxField)`
  margin-top: -20px;
`;
