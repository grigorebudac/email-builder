version: '3.3'

services:
  mongodb:
    image: prismagraphql/mongo-single-replica:4.4.3-bionic
    container_name: mongodb
    volumes:
      - database:/data/db
    ports:
      - 27017:27017
    restart: always

volumes:
  database:
